let age = 12;
let status = age >= 18 ? `You are an adult` : `You are a minor`;
console.log(status)

// ? = if condition is true 
// : = else


let loggedIn = false;
let profileStatus = loggedIn === true ? `You are logged in` : `You are signed out`;
console.log(profileStatus)

